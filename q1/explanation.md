q1 Environment Verification

whoami: Identified the current user as codespace.

groups: Displayed the groups associated with the current user account.

echo "$SHELL": Confirmed that the current shell is /bin/bash.

pwd: Verified the current working directory is /workspaces/Graded-assignment.

ls -ls: Listed the files and directories in the repository workspace.

Network test: Checked external connectivity to youtube.com and confirmed it is reachable.
